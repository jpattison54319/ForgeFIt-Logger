# FF-012 — ForgeData SwiftData models + round-trip 🟢

**Epic:** E1 · **Status:** 🟢 Completed · **Owner:** —

SwiftData `@Model` types mirroring the relational schema, with computed fields filled
from `ForgeCore` on save, and a local round-trip test.

## Acceptance
- [x] `WorkoutModel`, `WorkoutExerciseModel`, `SetModel`, `RoutineModel`, `RoutineExerciseModel`, `RoutineSetModel`, `ExerciseLibraryModel`, `UserExerciseNoteModel`
- [x] Client-generated UUID PKs (plain `UUID`, no `@Attribute(.unique)` — unsupported by CloudKit); cascade relationships
- [x] On save, `tonnage`/`effectiveLoad`/`est1RM` computed via `ForgeCore`
- [x] AC-7: a workout with a unilateral set + a drop set + a weighted-bodyweight set
      round-trips through SwiftData unchanged (persistence test)

## Verified
- `make test` runs 13 `ForgeCore` tests + `ForgeData` SwiftData persistence tests, then builds `ForgeHealth`, `ForgeWorkoutSession`, and `ForgeUI`.

## Depends on
- FF-010 (ForgeCore models) ✅
- FF-002 (ModelContainer) for app wiring (tests can use an in-memory container directly)
