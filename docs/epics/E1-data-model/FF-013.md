# FF-013 — CloudKit sync two-device check ⚪

**Epic:** E1 · **Status:** ⚪ Not Started · **Owner:** —

Verify that CloudKit sync works across two devices signed into the same iCloud account.

## Acceptance
- [ ] AC-8: workout created on device A appears on device B (same iCloud account)
- [ ] Routine created on one device appears on the other
- [ ] Exercise library seed syncs to a new device on first launch
- [ ] Delete on one device propagates to the other (tombstone, no resurrection)
- [ ] Offline creates sync automatically when connectivity returns

## Notes
- This is a manual test — CloudKit sync cannot be unit-tested without an iCloud account.
- Previously this card was "RLS integration test" for Supabase; that is no longer applicable
  since the backend was replaced by CloudKit. CloudKit's private database model enforces
  isolation per iCloud account — no RLS needed.

## Depends on
- FF-003 (CloudKit sync configured) ✅
