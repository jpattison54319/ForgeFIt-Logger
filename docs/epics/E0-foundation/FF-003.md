# FF-003 — CloudKit sync configured 🟢

**Epic:** E0 · **Status:** 🟢 Completed · **Owner:** —

Enable SwiftData + CloudKit sync for all `@Model` types via `cloudKitDatabase: .automatic`.

## Acceptance
- [x] `ModelConfiguration(cloudKitDatabase: .automatic)` set in `ForgeFitApp.swift`
- [x] iCloud + CloudKit entitlements added to `ForgeFit.entitlements`
- [x] `@Attribute(.unique)` removed from all 16 `@Model` IDs (unsupported by CloudKit)
- [x] App builds with CloudKit sync enabled (`make build-ios` succeeds)
- [x] iCloud capability enabled in Xcode → Signing & Capabilities (container `iCloud.org.xpetsllc.ForgeFit`)

## Notes
- Supabase sync layer (`SyncEngine`, `SyncPayloads`, `ForgeNetwork` package, `supabase/` backend dir) removed — CloudKit replaces it.
- `ck-environment` set to `Development`; flip to `Production` before App Store release.
- CloudKit sync itself cannot be unit-tested without an iCloud account; verification is manual (two-device check).
