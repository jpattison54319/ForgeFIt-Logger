# FF-004 — CI: build both targets + run tests 🟡

**Epic:** E0 · **Status:** 🟡 In Progress · **Owner:** —

Continuous integration that builds both app targets and runs the package test suites
on every PR.

## Acceptance
- [ ] CI workflow (GitHub Actions on a macOS runner) selects Xcode and runs `make test`
- [x] `ForgeCore` and `ForgeData` tests run locally; support packages build via `make test`
- [x] `xcodebuild` builds iOS + watchOS targets locally
- [ ] Status badge in root README

## Notes
- Test invocation must set the full Xcode toolchain (`DEVELOPER_DIR`) — see `Makefile`.
- Device-lab Watch tests (mirroring/disconnect) run pre-release, not on every PR.
