# FF-002 — SwiftData container + app shell + design tokens 🟢

**Epic:** E0 · **Status:** 🟢 Completed · **Owner:** —

Create the iOS and watchOS app targets, the SwiftData `ModelContainer`, the root tab
navigation, and the `ForgeUI` design-token foundation.

## Acceptance
- [x] iOS app target launches to a 5-tab shell (Home / Routines / History / Recovery / Profile)
- [x] watchOS app target launches to a workout shell
- [x] `ModelContainer` configured with the `ForgeData` schema and injected via environment
- [x] `ForgeUI` exposes color/spacing/typography tokens + shared component foundation
- [x] Health usage strings staged in generated Info.plist settings

## Notes
- The active app sources live in `ForgeFit/` and `ForgeFitWatch Watch App/` inside the
  manually-created Xcode project under `~/Developer/ForgeFit/ForgeFit`.
- Verified with `make build-ios` and `make build-watch`.
