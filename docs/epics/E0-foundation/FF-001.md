# FF-001 — Workspace + package layout 🟢

**Epic:** E0 · **Status:** 🟢 Completed · **Owner:** —

Establish the repository structure and the shared Swift package layout that both the
iOS and watchOS app targets will depend on.

## Acceptance
- [x] `git init`; `.gitignore` (Xcode/SPM/env)
- [x] `Packages/ForgeCore` SwiftPM package created and builds
- [x] `Makefile` with `build-core` / `test-core` using the Xcode toolchain
- [x] Stub packages `ForgeData`, `ForgeHealth`, `ForgeWorkoutSession`, `ForgeUI`
- [x] Root `ForgeFit.xcworkspace` referencing app targets + local packages

## Done so far
- `Packages/ForgeCore` builds; `swift-tools-version: 6.2`, platforms iOS/watchOS/macOS 26.
- `Packages/ForgeData`, `ForgeHealth`, `ForgeWorkoutSession`, and `ForgeUI`
  all build through `make test`; `ForgeData` now has the AC-7 persistence test.
- `ForgeFit.xcworkspace` references the manually-created `ForgeFit.xcodeproj`; the
  project references all local packages.
