# ForgeFit routine hierarchy preview

Private, phone-sized approval preview for the Workout tab's routine hierarchy.

The interactive page compares the current nested containers with the proposed
borderless section treatment across routines-only, one-folder, nested-cycle,
and mixed-library states. It also demonstrates accepted and rejected drop
feedback.

```bash
npm install
npm run dev
npm test
```
